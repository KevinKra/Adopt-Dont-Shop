<section class="page-template-1">
  <section class="pet-content">
    <% @pets.each do |pet| %>
      <article id="pet-<%= pet.id %>">
        <h2><%= pet.name %></h2>
        <img src=<%= pet.image %> />
        <p><%= pet.age %></p>
        <p><%= pet.sex %></p>
        <p><%= pet.shelter.title %></p>
        <%= link_to 'Edit', "/pets/#{pet.id}" %>
        <%= link_to 'Delete', "/pets/#{pet.id}", method: :delete %>
      </article>
    <% end %>
  </section>
</section>